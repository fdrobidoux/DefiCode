"use strict";

/**
 *
 */
function Parser(input: string) {
	// The full equation from the user.
	this.input = input;
	this.
}

/**
 * Extracts a binome from the given string.
 * @param index
 */
Parser.prototype.extractBinomeAt = function(index: number) {



}

/** @inheritDoc */
module.exports.Parser = Parser;